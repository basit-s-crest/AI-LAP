export interface CrisisResource {
  emoji: string;
  title: string;
  description: string;
  background: string;
}

export interface HelpfulLink {
  title: string;
  description: string;
}

export interface VideoResource {
  emoji: string;
  title: string;
  duration: string;
  background: string;
  tag: string;
}
